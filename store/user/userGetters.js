export const getters = {
   getCartItems: state => {
     return state.cart
   },
   getUser: state => {
     return state.user
   },
   getLocale: state => {
     return state.locale
   },
   getToken: state => {
     return state.token
   },
   getCartItems: state => {
     return state.cart
   },
   dropdownOpened: state => {
     return state.dropdownOpened
   }
 }
